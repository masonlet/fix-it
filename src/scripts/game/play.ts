import type { GameAssets } from "./assets.ts";
import type { GameState  } from "./types.ts";

export function updatePlayState(gameState: GameState, dt: number): boolean {
  // Belt

  // Spawner

  /// Minigame

  // Difficulty

  return false;
}

export function renderPlayState(
  ctx:       CanvasRenderingContext2D,
  gameState: GameState,
  assets:    GameAssets,
  w:         number,
  h:         number,
): void {
  // Belt

  // Items

  // Minigame

  // Hud
}
